<Application x:Class="diploma_app.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:diploma_app"
             StartupUri="MainWindow.xaml">
    <!-- закргуленный passwordbox -->
    <Application.Resources>
        <ControlTemplate x:Key="passwordbox" TargetType="{x:Type PasswordBox}">
            <Border Background="{TemplateBinding Background}"                     
                    x:Name="Bd" BorderBrush="#000000"
                    BorderThickness="1" CornerRadius="5, 0, 0, 5">
                <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
            </Border>
        </ControlTemplate>
    </Application.Resources>
</Application>
